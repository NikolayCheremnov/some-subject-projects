{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "UseConnection": "LocalFromHostToDocker",
  "ConnectionStrings": {
    "LocalFromHostToDocker": "Host=localhost;Port=5432;Username=postgres;Password=postgres;Database=requests_db",
    "LocalFromDockerToDocker": "Host=<your db service section name>;Port=5432;Username=postgres;Password=postgres;Database=requests_db"
  }
}
